spring:
  datasource:
    url: jdbc:mysql://mysql:3306/task_service

eureka:
  instance:
    prefer-ip-address: false
    hostname: task-service
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8070/eureka/
